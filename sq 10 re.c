#include<stdio.h>
int sumofsquare (int)
int main()
{
	int a;
	printf("enter a=");
	scanf("%d",&a);
	printf( "%d","sum of square(a)");
	return 0;
} int sumofsquare (int n);
{
int sum=0 ;
for (int i=1 ;i<=n;i++)}
{
	sum=sum +(i*i)}
	return sum ;
}
